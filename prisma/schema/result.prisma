model Result {
    id        Int      @id @default(autoincrement())
    name      String
    size      Int
    career    String
    score     Float
    imageUrl  String
    createdAt DateTime @default(now())
}
